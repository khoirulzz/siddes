<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+ system splash, matched to the in-app cinematic intro. -->
    <style name="Theme.SIDMobile.Splash" parent="Theme.SIDMobile">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_logo_padded</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
    </style>

</resources>
