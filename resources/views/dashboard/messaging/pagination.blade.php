<div class="table-pagination"><small class="muted">{{ $paginator->firstItem() ?? 0 }}–{{ $paginator->lastItem() ?? 0 }} dari {{ $paginator->total() }}</small><div class="pager-controls">@if($paginator->previousPageUrl())<a class="pager-link" href="{{ $paginator->previousPageUrl() }}">Sebelumnya</a>@endif<span class="pager-meta">Halaman {{ $paginator->currentPage() }}/{{ $paginator->lastPage() }}</span>@if($paginator->nextPageUrl())<a class="pager-link" href="{{ $paginator->nextPageUrl() }}">Berikutnya</a>@endif</div></div>
