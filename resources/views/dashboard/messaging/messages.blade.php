<div class="messaging-table"><table><thead><tr><th>Penerima</th><th>Pesan</th><th>Proses</th><th>Pengantaran</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $item)<tr><td>+{{ $item['recipient'] }}</td><td class="messaging-message">{{ $item['renderedMessage'] }}@if($item['errorMessage'])<small class="messaging-error">{{ $item['errorMessage'] }}</small>@endif</td><td>{{ \App\Support\MessagingLabels::status($item['status']) }}<small class="muted">Percobaan {{ $item['attempts'] }}/{{ $item['maxAttempts'] }}</small></td><td>{{ \App\Support\MessagingLabels::status($item['deliveryStatus']) }}</td><td>@if($item['status']==='FAILED' || ($item['status']==='SENT' && in_array($item['deliveryStatus'], ['ERROR','UNKNOWN'])))<form method="POST" action="{{ route('dashboard.messaging.messages.retry', $item['id']) }}" data-confirm="{{ $item['deliveryStatus']==='UNKNOWN' ? 'Pengiriman belum pasti. Retry dapat menyebabkan pesan ganda. Lanjutkan?' : 'Antrekan ulang pesan ini?' }}">@csrf<button class="btn btn-secondary">Antrekan ulang</button></form>@endif</td></tr>@empty<tr><td colspan="5">Belum ada pesan yang cocok.</td></tr>@endforelse</tbody></table></div><p class="muted">Pesan tidak pasti tidak dikirim ulang otomatis. Periksa penerima sebelum memilih retry.</p>
