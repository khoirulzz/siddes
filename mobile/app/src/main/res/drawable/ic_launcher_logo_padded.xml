<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Logo already has ~20% internal margin, so a small inset is sufficient -->
    <item
        android:drawable="@drawable/logo_sid"
        android:gravity="center"
        android:width="56dp"
        android:height="56dp" />
</layer-list>
