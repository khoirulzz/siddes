<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Match the Compose splash palette to avoid a bright flash on cold start. -->
    <item android:drawable="@color/splash_background" />
    <!-- Compact official crest shown while the app process is starting. -->
    <item
        android:gravity="center"
        android:width="92dp"
        android:height="92dp"
        android:drawable="@drawable/logo_sid" />
</layer-list>
